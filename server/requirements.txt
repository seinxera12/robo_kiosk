# Server requirements for voice kiosk chatbot

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# AI/ML models
faster-whisper==0.10.0
torch==2.1.0
sentence-transformers==2.2.2
openai==1.3.0

# Vector database
chromadb==0.4.18

# HTTP client
httpx==0.25.1

# Audio processing
numpy==1.24.3
soundfile==0.12.1

# TTS engines
# Note: CosyVoice now runs as separate service (see COSYVOICE_SERVICE_SETUP.md)
# Kokoro-82M: local English TTS (primary engine)
kokoro>=0.9.4
misaki[en]>=0.9.4   # English G2P / phonemiser required by Kokoro

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
